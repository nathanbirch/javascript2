function computeCircleCircumference(radius) {
  return radius * Math.PI * 2;
}
function computeCircleArea(radius) {
  return Math.PI * radius ** 2;
}

export { computeCircleArea, computeCircleCircumference };
